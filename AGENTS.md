# marka.md App Notes

- This is the Tauri app repo for `mattenarle10/markamd`.
- Keep changes scoped; do not touch release artifacts or generated `dist/` unless explicitly asked.
- Run `bun run build` after meaningful TypeScript, React, CSS, or markdown-rendering changes.
- The app is local-first: no telemetry, no accounts, no cloud sync.
- Product copy should stay casual, direct, and close to Matt's voice.
- Use "downloads" for GitHub release asset counts, not "installs".
- Prefer Conventional Commits matching repo history, e.g. `fix(pdf): preserve page margins across print pages`.
